import {useEffect,useRef,useState} from "react";import {motion,useInView} from "framer-motion";const stats=[{value:5,label:"Projects",detail:"Practical builds"},{value:10,label:"Technologies",detail:"Modern tech stack"},{value:6,label:"Career Paths",detail:"Focused opportunities"},{value:15,label:"Learning Opportunities",detail:"Courses & programs"}];function Counter({value}){const ref=useRef(null),inView=useInView(ref,{once:true,amount:.7}),[count,setCount]=useState(0);useEffect(()=>{if(!inView)return;const duration=950,startTime=performance.now();const tick=now=>{const p=Math.min((now-startTime)/duration,1),e=1-Math.pow(1-p,3);setCount(Math.round(value*e));if(p<1)requestAnimationFrame(tick)};requestAnimationFrame(tick)},[inView,value]);return <span ref={ref}>{count}+</span>}function StatsSection(){return <section className="qk-stats-section"><div className="container"><div className="qk-stats-wrapper">{stats.map((stat,i)=><motion.div className="qk-stat" key={stat.label} initial={{opacity:0,y:18}} whileInView={{opacity:1,y:0}} viewport={{once:true,amount:.4}} transition={{delay:i*.08,duration:.55}}><span className="qk-stat-symbol"><Counter value={stat.value}/></span><strong>{stat.label}</strong><p>{stat.detail}</p></motion.div>)}</div></div></section>}export default StatsSection;
