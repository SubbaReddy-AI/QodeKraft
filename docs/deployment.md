# QodeKraft Deployment Guide

## Requirements

Install:

- Docker Desktop
- Docker Compose
- Git

## Environment Configuration

Copy the environment examples before running the application.

```text
.env.example        -> .env
backend/.env.example -> backend/.env